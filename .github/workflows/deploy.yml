name: 67 design and build API CI/CD Pipeline

on:
  push:
    # branches:
    #   - main
    #   - dev

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint

  build:
    name: Build API
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type Check
        run: npx tsc --noEmit

      - name: Build project
        run: npm run build

# name: Kisuma Api CI/CD Pipeline

# on:
#   push:
#     branches:
#       - main
#       - dev

# jobs:
#   lint:
#     name: Lint Code
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install deps
#         run: npm ci
#       - name: Run linter
#         run: npm run lint

#   build:
#     name: Build API
#     runs-on: ubuntu-latest
#     needs: lint
#     steps:
#       - uses: actions/checkout@v4
#       - name: Install deps
#         run: npm ci
#       - name: Build project
#         run: npm run build
